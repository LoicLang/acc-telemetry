---
summary: authorized bounded Astra visual trial, exact comparison with root pixel references and limits before sustained counter review
read_when:
  - deciding how to resume counter review after the run-018 reliability failures
  - interpreting the Astra trial or preparing its next controlled batch
---

> Historical evidence only — archived13 September2026. Old instructions, gates and next actions below are not current. Follow [current status](../../current-status.md).

# Bounded Astra counter-review trial — 11 September 2026

The owner explicitly authorized an Astra sub-agent experiment after discussing the
failed Sol reviews. One **gpt-6-astra, high reasoning** agent inspected a fixed small
lot. This is an exception for the bounded visual trial, not a general model-policy
change or a full-source acceptance decision. No other sub-agent or recursive delegation
was used. Outputs are exclusively under ignored `data/lab/coaching-reliability/run-019/`.

## Frozen scope and reference

`reports/trial-manifest.json` was fixed before the review: **14 original sheets and
11 native crops**, covering stable portions, the eight previously located transitions
and disputed frames from earlier failed reviews. These are existing development media;
source/video SHA and size, index/dossier binding and image SHA/size were verified.
No video, OCR, profile, threshold or application code changed.

The lot contains **1,355 presentations of 1,348 distinct source frames**. Seven frames
appear both on a sheet and as a native crop; they are not independent extra examples.
Many adjacent cells are also strongly related. These counts must not be described
as independent validation samples or additional recordings.

The protocol uses one explicitly identified image per tool result and `original`
detail in both `view_image` and the image emitter. Every displayed cell must be
inspected. Any ambiguity requires an individually identified native-crop check and
remains unknown if unresolved. No OCR or predicted number supplies annotation truth.

Root's reference in `reports/reference-root.json` reuses its preserved 960-cell pixel
review and explicit native-crop checks, with four additional original sheets reinspected
before the trial result was received. The trial agent was instructed not to read this
reference. It disclosed that mandatory startup documents exposed a few known failure
values and boundary facts; therefore this is not fully blinded to all prior knowledge.
The reference is an agent pixel review, not a new independent human approval.

## Results

| Check | Result |
| --- | ---: |
| Requested image items inspected | 25/25 |
| Literal counter and visibility agreements | 1,355/1,355 |
| Disagreements | 0 |
| Visible transition brackets exactly matched | 8/8 |
| Disputed native crops correctly read | 11/11 |
| Reported absent or unknown presentations | 0 |
| Extra uncertainty inspections needed | 0 |

`reports/astra-trial.json` preserves the reviewer's own item-by-item ranges, hashes,
counts, method, prior-knowledge disclosure and limitations.
`reports/trial-results.json` expands and compares every frame presentation, checks
complete range coverage and duplicate consistency, and verifies all eight brackets.

**The bounded trial passes.** The absence/unknown misreadings from the earlier failed
reviews did not recur on this lot. This supports a controlled next batch; it does not
prove that the cause of the previous errors has been identified or permanently removed.

## Limits and next action

Sol also passed a short trial before failing sustained review. A successful small
Astra trial cannot qualify all 803 sheets. This lot contains readable reference cases;
it checks false-absence claims but does not measure recognition of genuinely absent
HUD. It introduces no new recording, circuit or independent generalization test.

**Gate A remains FAIL.** Exhaustive event recall remains not_evaluated; existing
coverage, field-quality and independent-recording requirements remain open. No B/R,
holdout, 720p processing or new telemetry replay was performed. Previous failed reviews
remain intact and must not be imported as truth.

Final code/config and prior-evidence preservation, focused/full tests, docs discovery
and diff check are recorded in `reports/final-verification.json`. Trial fingerprint:
`e673532f2a9c46979d54b3c2605def41cd8aee26850d9b702ba5bdc9323d6520`.

Next action: freeze a contiguous **first 40 BMW sheets (frames 0–3839)** with native
QA sentinels selected and read before the next pass, then evaluate sustained reading
before extending the review. Preserve exact per-sheet evidence and stop on a confirmed
misreading; never convert a short pilot result into exhaustive truth.

The subsequent sustained run-020 attempt did not qualify. Exact-pixel mapping and
its partial primary review are documented in
[exact-counter-state-review.md](exact-counter-state-review.md); no broad reliability
claim is transferred from this successful small trial.
